## Packages
framer-motion | Page transitions and scroll-triggered animations
lucide-react | Beautiful, consistent icons for the crypto aesthetic
date-fns | Formatting transaction dates

## Notes
Tailwind Config - Please ensure the following fonts are added if tailwind.config.ts is editable:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  display: ["Outfit", "sans-serif"],
  mono: ["JetBrains Mono", "monospace"],
}
We use space-separated HSL values for the luxury dark crypto theme.
Authentication uses a hidden /admin-login route with session cookies.
